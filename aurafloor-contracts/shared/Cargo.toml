[package]
name = "shared"
version = "0.1.0"
description = "Shared types and utilities for AuraFloor contracts"
authors = ["AuraFloor Team"]
edition = "2021"
publish = false

[lib]
crate-type = ["rlib"]

[dependencies]
soroban-sdk = "23.0.1"

[features]
testutils = ["soroban-sdk/testutils"]
